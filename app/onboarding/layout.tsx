export const metadata = {
  title: "Onboarding",
  description: "Set up and personalize your Kora workspace.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
